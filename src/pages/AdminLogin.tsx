import { useEffect, useState } from 'react';
import { motion, useReducedMotion } from 'framer-motion';
import { useNavigate } from 'react-router-dom';
import { useAuth } from '../auth/AuthContext';
import { ThemeToggle } from '../components/ui/ThemeToggle';

export function AdminLogin(){const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[error,setError]=useState('');const[loading,setLoading]=useState(false);const navigate=useNavigate();const{user,refresh}=useAuth();const reduce=useReducedMotion();useEffect(()=>{if(user)navigate(user.role==='admin'?'/admin':'/home',{replace:true})},[user,navigate]);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError('');await new Promise(r=>setTimeout(r,500));if(email.trim().toLowerCase()!=='gustavo@metastrategy.co'||password!=='Meta@2026'){setError('Credenciais administrativas incorretas.');setLoading(false);return}localStorage.setItem('meta_session_demo',JSON.stringify({id:1,email,name:'Gustavo',role:'admin'}));await refresh();navigate('/admin',{replace:true})}return <main className="min-h-screen bg-[#0b0b0a] text-[#f2eee6] grid lg:grid-cols-[1.05fr_.95fr] relative overflow-hidden"><ThemeToggle className="absolute right-6 top-6 z-20"/><section className="hidden lg:flex border-r border-white/10 p-16 xl:p-24 flex-col justify-between relative"><div className="absolute inset-0 bg-[radial-gradient(circle_at_20%_20%,rgba(166,143,99,.13),transparent_40%)]"/><p className="editorial-brand text-2xl relative">Meta Strategy</p><div className="relative"><p className="text-[#a68f63] text-[10px] tracking-[.3em] uppercase mb-6">Área do consultor</p><h1 className="editorial-serif text-7xl xl:text-8xl leading-[.88] tracking-[-.05em]">Gestão que<br/>antecipa o<br/><i className="text-white/35">movimento.</i></h1></div><p className="text-white/25 text-xs tracking-[.14em] uppercase relative">Ambiente administrativo privado</p></section><section className="flex items-center justify-center px-6 py-20"><motion.div initial={reduce?false:{opacity:0,y:18}} animate={{opacity:1,y:0}} className="w-full max-w-[380px]"><div className="lg:hidden editorial-brand text-2xl mb-20">Meta Strategy</div><p className="text-[#a68f63] text-[9px] tracking-[.28em] uppercase mb-4">Acesso restrito</p><h2 className="editorial-serif text-5xl mb-3">Olá, Gustavo.</h2><p className="text-white/40 text-sm leading-relaxed mb-10">Entre para acompanhar clientes, conteúdos e o avanço de cada consultoria.</p><form onSubmit={submit} className="space-y-6"><input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="E-mail administrativo" required className="glass-input !text-white !border-white/20"/><input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Senha" required className="glass-input !text-white !border-white/20"/><div className="min-h-6 text-sm text-red-300">{error}</div><button disabled={loading} className="w-full h-14 rounded-full bg-[#eee9df] text-black text-[10px] font-semibold tracking-[.2em] uppercase">{loading?'Entrando...':'Entrar no painel'}</button></form><button onClick={()=>navigate('/login')} className="mt-8 text-[10px] tracking-[.14em] uppercase text-white/30 hover:text-white">Ir para o acesso do cliente</button></motion.div></section></main>}
